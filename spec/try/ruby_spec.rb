# frozen_string_literal: true

RSpec.describe Try::Ruby do
  it 'has a version number' do
    expect(Try::Ruby::VERSION).to eq('0.1.0')
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
